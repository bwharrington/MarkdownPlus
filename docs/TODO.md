- Markdown Mode - Takes a non markdown file and converts it to markdown.

- Still seeing weird cursor jumping when typing. Maybe related to the fact that we are re-rendering the entire editor on every keystroke? Maybe we can optimize this by only re-rendering the changed line or something?

- Performance is terrible on very large file. Figure out why. Sometimes there are crashes. See file in docs.

- Support image attachments. See logs in docs.

- Fix Mermaid diagram. See file in docs.